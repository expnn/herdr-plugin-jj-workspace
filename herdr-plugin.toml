id = "nathanflurry.jj-workspace"
name = "jj workspaces"
version = "0.3.0"
min_herdr_version = "0.7.0"
description = "Create jj workspaces with Codex on the left and a terminal on the right."
platforms = ["linux", "macos"]

# Built at install time (needs cargo). `plugin link` does not build, so local
# devs run `cargo build --release` themselves.
[[build]]
command = ["cargo", "build", "--release"]

# Open a new jj workspace as a tab in the selected Herdr workspace.
[[actions]]
id = "new"
title = "New jj workspace"
contexts = ["workspace", "global"]
command = ["./target/release/jj-workspace", "open", "tab"]

# Compatibility alias for existing prefix+a configurations.
[[actions]]
id = "new-tab"
title = "New jj workspace"
contexts = ["workspace", "global"]
command = ["./target/release/jj-workspace", "open", "tab"]

# Interactive source-workspace + name selector opened by both actions.
[[panes]]
id = "wizard"
title = "New jj workspace"
placement = "overlay"
command = ["./target/release/jj-workspace", "wizard"]

# Remove the focused jj workspace.
[[actions]]
id = "remove"
title = "Remove jj workspace"
contexts = ["workspace"]
command = ["./target/release/jj-workspace", "remove"]
